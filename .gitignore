# 临时文件
*.tmp
*.swp
*.log.tmp
.DS_Store

# Git自身
.git/

# 运行时缓存
__pycache__/
*.pyc

# 密钥与凭证（永不提交）
# 注意：identity/目录下的身份凭证为哈希凭证，不含明文密钥
# 实际令牌存储于 ~/.git-credentials，不在本仓库内
*.key
*.pem
*token*
*secret*
*credential_plain*

# 公开镜像仓导出时需额外排除：
# identity/
# kernel_asset_lock_config.json
# kunlun-assets/
# 以上为内核私有层，公开展示层仅包含 README.md / demo-showcase/ / business-docs/ / visual-docs/
