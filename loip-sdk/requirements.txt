# LOIP SDK 核心依赖（零依赖，仅Python标准库）
# 以下为可选依赖，按需安装

# REST API 服务（loip serve / api_server 需要）
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# 语义级检测（可选，backend=semantic 时需要）
# sentence-transformers>=2.2.0
# transformers>=4.30.0
# torch>=2.0.0

# 开发工具
# pytest>=7.0.0
# black>=23.0.0
